$if(parentheses)$
\PassOptionsToPackage{autocite=plain}{biblatex}
$endif$
\documentclass
[
$if(proofs)$
stage=draft,
$else$
stage=final,
$endif$
$if(issue.bookauthors)$
discussed-book-authors={$issue.bookauthors$},
discussed-book={$issue.title$},
$else$
$if(issue.title)$
issue={$issue.title$},
$endif$
$endif$
$if(issue.editors)$
editor={$issue.editors$},
$endif$
volume=$volume$,
$if(doi)$
doi=$doi$,
$else$
doi=10.33735/phimisci.$date$.$artid$,
$endif$
year=$date$,
$if(widows)$
settings/widow-control=chernoff
$endif$
]
{tex/phimisci}

\PhiMiSciSettings{
  settings/logo-url={images/phimisci-logo-mini.pdf},
  settings/copyright-text={%
        \href{https://philosophymindscience.org}{philosophymindscience.org}.
        ISSN: 2699-0369.\hfill
        \href{https://creativecommons.org/licenses/by/4.0}{\includegraphics[width=1.75cm]{images/ccby.pdf}}%
      }
}

\title$if(short-title)$[$short-title$]$endif${$title$}
  
$if(subtitle)$\subtitle{$subtitle$}$endif$
      
\author{$for(author)$$author.name$$if(author.affiliation)$\affiliation{$for(author.affiliation)$$author.affiliation.organization$$sep$ ; $endfor$}$endif$$if(author.orcid)$\orcid{$author.orcid$}$endif$
$sep$\and $endfor$}

$if(tags)$
\keywords{$for(tags)$$tags$$sep$;$endfor$}
$endif$       

$if(abstract)$
\abstract{$abstract$}
$endif$

$if(contact)$
\contact{$contact.name$}{$contact.email$}
$endif$

$if(bibliography)$
\addbibresource{$bibliography$}
$endif$

\hypersetup{pdfinfo = {/Producer (PhiMiSci Typesetting Container)}}

\begin{document}
\maketitle
$body$

\printbibliography
\end{document}
