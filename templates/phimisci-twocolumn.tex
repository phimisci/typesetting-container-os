\documentclass
[
$if(stage)$
stage=$stage$,
$else$
stage=final,
$endif$
$if(issue.title)$
issue={$issue.title$},
$endif$
$if(issue.editors)$
editor={$issue.editors$},
$endif$
volume=$volume$,
$if(doi)$
doi=$doi$,
$else$
doi=10.33735/phimisci.$date$.$artid$,
$endif$
year=$date$,
settings/logo-url={$logo$}
]
{phimisci}

\title$if(short-title)$[$short-title$]$endif${$title$}
  
$if(subtitle)$\subtitle{$subtitle$}$endif$
      
\author{$for(author)$$author.name$
$if(author.affiliation)$
\affiliation{$for(author.affiliation)$
$author.affiliation.organization$$sep$ ; $endfor$}
$endif$
$if(author.orcid)$\orcid{$author.orcid$}$endif$
$sep$ \and
$endfor$}

$if(tags)$
\keywords{$for(tags)$$tags$$sep$;$endfor$}
$endif$       

$if(abstract)$
\abstract{$abstract$}
$endif$

$if(contact)$
\contact{$contact.name$}{$contact.email$}
$endif$

$if(bibliography)$
\addbibresource{$bibliography$}
$endif$

\pdfinfo{/Producer (PhiMiSci Typesetting Container)}

\begin{document}
\maketitle
$body$
\end{document}
